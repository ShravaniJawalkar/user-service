

spring:
  config:
    import: optional:http://dummy-config-server
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: password
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false